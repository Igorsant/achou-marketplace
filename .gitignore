# dependencias e build (qualquer nivel do monorepo)
node_modules/
dist/
build/

# ambiente
.env
.env.local

# logs e temporarios
*.log
.DS_Store

# Terraform: o state guarda senhas e o token do registro em texto puro.
# O .terraform.lock.hcl VAI para o git (fixa as versoes dos providers).
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
infra/oci/kubeconfig

# Flutter (quando o app entrar)
.dart_tool/
.packages
